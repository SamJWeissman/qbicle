{
  "enabledPlugins": {
    "superpowers@claude-plugins-official": true
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; ext=\"${f##*.}\"; if echo \"$ext\" | grep -qE '^(js|cjs|mjs)$'; then npm run lint 2>&1 && npm test 2>&1; fi; } || true",
            "statusMessage": "Linting and running tests..."
          }
        ]
      }
    ]
  }
}
